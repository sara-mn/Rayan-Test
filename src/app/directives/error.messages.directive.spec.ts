import { ErrorMessagesDirective } from './error.messages.directive';

describe('Error.MessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorMessagesDirective();
    expect(directive).toBeTruthy();
  });
});
